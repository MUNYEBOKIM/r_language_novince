data("pressure") #pressure dataset을 로드한다. 
print(head(pressure)) #pressure 데이터의 앞 6개 행을 출력해준다. 
plot(pressure)
grid()
#console에 data()라고 입력하면 어떤 함수가 있는지 확인할 수 있다.
data("WorldPhones")
print(WorldPhones)
hist(WorldPhones)
gride()
